      - name: Commit and Push Market Data
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sentinel_market_data.csv
          # Wir erzwingen den Commit, selbst wenn Git meint, es gäbe nichts
          git commit -m "🛡️ Aureum Update: $(date +'%H:%M:%S')" || echo "No changes"
          # Force Push, um eventuelle Branch-Konflikte zu überspringen
          git push origin main --force
