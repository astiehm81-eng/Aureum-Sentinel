      - name: Commit and Push Market Data
        run: |
          git config --global user.name "Aureum-Sentinel-Bot"
          git config --global user.email "sentinel@aureum.de"
          
          # Sicherstellen, dass wir auf main sind
          git checkout main
          
          # Debug: Zeige uns, ob die Datei überhaupt existiert und wie groß sie ist
          ls -lh sentinel_market_data.csv || echo "DATEI NICHT GEFUNDEN"
          
          # Force Add
          git add -f sentinel_market_data.csv
          
          # Commit mit Zeitstempel erzwingen
          git commit -m "🛡️ Aureum V52 Fix: $(date +'%H:%M:%S')" || echo "No changes to commit"
          
          # Push mit explizitem Ziel
          git push origin main
