name: Aureum Sentinel Perpetual Loop

on:
  schedule:
    - cron: '*/20 * * * *' # Alle 20 Minuten (Gibt GitHub 5 Min Zeit zum Committen)
  workflow_dispatch:      # Manueller Start erlaubt

# WICHTIG: Stoppt sofortige Mehrfach-Runs
concurrency:
  group: sentinel-run
  cancel-in-progress: false

jobs:
  sentinel-cycle:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 📥 Repository laden
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Python Setup
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Pakete
        run: pip install pandas yfinance pyarrow fastparquet google-generativeai

      - name: 🔍 Phase 1 - Finder (Wachstum)
        continue-on-error: true
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python aureum_gemini_finder.py

      - name: 🛡️ Phase 2 - Sentinel (15 Min Datensammlung)
        run: python aureum_sentinel.py

      - name: 💾 Phase 3 - Datenbank-Sync & Commit
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Aureum Sentinel"
          git pull --rebase origin main
          git add heritage_vault/*.parquet anchors_memory.json isin_pool.json || true
          git commit -m "Cycle-Update: $(date +'%H:%M') [skip ci]" || echo "No changes"
          git push origin main
