name: Aureum Sentinel Perpetual Loop

on:
  schedule:
    - cron: '*/20 * * * *'
  workflow_dispatch:

concurrency:
  group: sentinel-run
  cancel-in-progress: false

jobs:
  sentinel-cycle:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 📥 Repository laden
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Python Setup
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Pakete installieren
        run: pip install pandas yfinance pyarrow fastparquet google-genai

      - name: 🛡️ Phase 1 & 2 - Aureum Multi-Process Execution
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python aureum_sentinel.py

      - name: 💾 Phase 3 - Datenbank-Sync & Commit
        if: always()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Aureum Sentinel"
          git pull --rebase origin main
          git add heritage_vault/*.parquet anchors_memory.json isin_pool.json coverage_report.txt || true
          git commit -m "Cycle-Update: $(date +'%H:%M') [skip ci]" || echo "No changes"
          git push origin main
